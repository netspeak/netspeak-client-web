{
  "name": "netspeak",
  "description": "Netspeak web interface",
  "private": true,
  "scripts": {
    "test": "npm run lint && polymer test",
    "build": "polymer build",
    "serve": "npm run build && polymer serve build/default",
    "lint": "npx eslint ./"
  },
  "dependencies": {
    "@polymer/polymer": "^3.0.0"
  },
  "devDependencies": {
    "@polymer/test-fixture": "^4.0.2",
    "@webcomponents/webcomponentsjs": "^2.0.0",
    "babel-eslint": "^10.0.1",
    "chai": "^4.2.0",
    "eslint": "^5.16.0",
    "mocha": "^5.2.0",
    "wct-mocha": "^1.0.0"
  }
}
